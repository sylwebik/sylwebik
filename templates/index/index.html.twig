{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Index ✅</h1>

        <a href="{{ path('admin') }}">Admin</a>
        </br>
        <a href="{{ path('course_index') }}">Twoje kursy</a>
        </br>
        <a href="{{ path('login') }}">Logowanie</a>
        </br>
        <a href="{{ path('profile') }}">Profil</a>
        </br>
        <a href="{{ path('subject_index') }}">Przedmioty</a>
        </br>
        <a href="{{ path('register') }}">Rejestracja</a>
        </br>
        <a href="{{ path('type_index') }}">Typy</a>
        </br>
        <a href="{{ path('conversation_index') }}">Wiadomości</a>
        </br>
        <a href="{{ path('course_search') }}">Wszystkie kursy</a>
        </br>
        <a href="{{ path('logout') }}">Wylogowanie</a>
    </div>
{% endblock %}
